<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= userId %></title>
</head>
<body>
    <form action="/user/<%= userId %>/updateExpense/<%= data.id %>" method="post">
        <input type="hidden" name="_csrf" value="<%=csrfToken%>">
        <label for="amount">Amount:</label>
        <input type="number" id="amount" name="amount" value="<%= data.amount %>" required>
        <div id="date-container">
            <label for="datetime">Date and month</label>
            <input type="datetime-local" name="datetime" value="<%= dateString %>"  required>
        </div>
        <div id="mode-container">
            <input type="radio" name="mode" id="card" value="card" <%= data.payment == "card" ? "checked" : "" %> required>
            <label for="card">Card</label>  

            <input type="radio" name="mode" <%= data.payment == "cash" ? "checked" : "" %> id="cash" value="cash">
            <label for="cash">Cash</label>
        </div>
        
        <div id="category-container">
            <input type="radio" name="category" id="food" value="food" <%= data.category == "food" ? "checked" : "" %> required>
            <label for="food">Food</label>

            <input type="radio" name="category" id="shopping" <%= data.category == "shopping" ? "checked" : "" %> value="shopping">
            <label for="shopping">shopping</label>

            <input type="radio" id="education" name="category" <%= data.category == "education" ? "checked" : "" %> value="education">
            <label for="education">education</label>

            <input type="radio" id="others" name="category" <%= data.category == "others" ? "checked" : "" %> value="others">
            <label for="others">others</label>

        </div>
        <div id="description-container">
            <label for="description">description</label>
            <textarea id="description" rows="5" cols="5" name="description"><%=data.description %>
            </textarea>
        </div>

        <button type="submit">Submit</button>
    </form>
</body>
</html>